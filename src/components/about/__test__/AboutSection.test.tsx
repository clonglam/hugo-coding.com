import { findByRole, render, screen } from "@testing-library/react"
import AboutSection from "../AboutSection"

test("should render About section", () => {
  expect(true).toBeTruthy()
  // render(<AboutSection />)

  // expect(screen.getByText(new RegExp(/hugo lam/i))).toBeInTheDocument()
  // expect(
  //   screen.getByText(new RegExp(/Front-end Developer/i))
  // ).toBeInTheDocument()
})
